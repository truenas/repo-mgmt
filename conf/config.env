#!/bin/sh
#
# Sourced by build.sh, set options for this branch/build of TrueNAS within

# What build is this? stable/unstable/release
TRUENASBUILD="unstable"

# Location for debian packages we pull from upstream
DEBMIRROR="/srv/mirror/debian-truenas-${TRUENASBUILD}"

# Where to pull upstream debian packages from
DEBREPO="deb.debian.org"

# What debian distribution to sync
DEBDIST="bullseye"

# Location for TrueNAS packages we pull down from mini-buildd
TNMIRROR="/srv/mirror/buildd-truenas-${TRUENAS_BUILD}"

# Where to build TN deb packages from
TNREPO="buildd.tn.ixsystems.com:8066"

# What truenas distribution to sync
TNDIST="bullseye-truenas-unstable"
